{% extends './scaffolds/public_scaffold.twig' %}

{% block body %}
<div class="v-centered main column">

    <div class="narrow h-centered container"
         xmlns:v-on="http://www.w3.org/1999/xhtml">

        <form class="form" id="login" method="POST" @submit="checkForm">

            <div class="avatar field">
                <img v-if="!avatar" src="{{ gravatar('a@a.a') }}" alt="Placeholder avatar" class="avatar">
                <img v-if="avatar" :src="avatar" alt="User avatar" class="avatar">
            </div>

            <div id="messages">

                {% if message %}
                    <div class="success timed message field">
                        <p>{{ message }}</p>
                    </div>
                {% endif %}

                <div class="error message field" v-if="errors.length" v-on:click="errors = []">
                    <b>Please correct the following error(s):</b>
                    <ul>
                        <li v-for="error in errors">${ error }</li>
                    </ul>
                </div>

                {% if messages %}
                    <div class="error message field" v-if="!errors.length">
                        <b>Please correct the following error(s):</b>
                        <ul>
                            {% for m in messages %}
                                <li>{{ m }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}
            </div>

            <div class="field">
                <label for="login">Login</label>
                <input type="text" name="login" id="login" v-model="login" v-on:focusout="getInfo" required>
            </div>

            <div class="field">
                <label for="password">Password</label>
                <input type="password" name="password" id="password" v-model="pass" required>
            </div>

            <div class="field" v-if="mfa">
                <label for="mfa">2FA token</label>
                <input type="password" name="mfa" id="mfa" required>
            </div>

            <div class="checkbox field">
                <label for="remember">Remember me</label>
                <input id="remember" name="remember" v-model="remember" type="checkbox">
            </div>

            <input id="token" type="hidden" name="token" value="{{ token }}">

            <div class="field">
                <label for="submit" class="hidden">Log in</label>
                <input id="submit" class="submit button" type="submit" value="Log in">
            </div>

        </form>

    </div>
</div>

    <script type="application/javascript" src="{{ vasset('js/v-login-validator', 'min.js') }}"></script>

{% endblock %}
